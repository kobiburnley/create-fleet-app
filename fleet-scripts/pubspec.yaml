name: fleet_scripts
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  cli_pkg: ^1.0.0-beta.9
  googleapis: ^0.56.0
  googleapis_auth: ^0.2.12
  googleapis_beta: ^0.51.0
#  inject:
#    git:
#      url: https://github.com/google/inject.dart
#      path: package/inject

  grinder: ^0.8.5
  medjool:
    git: https://github.com/kobiburnley/medjool
  mobx: ^1.2.1+3
  mobx_codegen: ^1.1.1+1
  path: ^1.7.0
  prompts: ^1.3.1
  pubspec_parse: ^0.1.5
  xml: ^3.6.0

dev_dependencies:
  build_runner: ^1.10.3
  effective_dart: ^1.2.4
#  inject_generator:
#    git:
#      url: https://github.com/google/inject.dart
#      path: package/inject_generator
  test: ^1.5.0
